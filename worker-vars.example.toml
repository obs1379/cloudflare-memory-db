# Copy this file for local reference only. Do NOT put real values in wrangler.toml.
# Configure production values with `wrangler secret put <NAME>`.

APP_AUTH_TOKEN="change-me-random-token"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="change-me-strong-password"
SESSION_SECRET="change-me-random-session-secret"
ENCRYPTION_KEY="change-me-keep-stable-after-first-deploy"
